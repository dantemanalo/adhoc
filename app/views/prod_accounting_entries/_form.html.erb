<%= form_with(model: prod_accounting_entry, local: true) do |form| %>
  <% if prod_accounting_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prod_accounting_entry.errors.count, "error") %> prohibited this prod_accounting_entry from being saved:</h2>

      <ul>
      <% prod_accounting_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
