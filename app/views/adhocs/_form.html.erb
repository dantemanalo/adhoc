<%= form_with(model: adhoc, local: true) do |form| %>
  <% if adhoc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(adhoc.errors.count, "error") %> prohibited this adhoc from being saved:</h2>

      <ul>
      <% adhoc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
